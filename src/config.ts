export const DIFFICULTY: number = parseInt(process.env.DIFFICULTY, 10) || 3;
export const MINE_RATE: number = parseInt(process.env.MINE_RATE, 10) || 3000;
export const INITIAL_BALANCE: number = parseInt(process.env.INITIAL_BALANCE, 10) || 500;
export const MINING_REWARD: number = parseInt(process.env.MINING_REWARD, 10) || 50;
